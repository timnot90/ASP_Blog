@using Blog.Web.Models
@using Blog.Web.Models.Shared
@model Blog.Web.Models.Account.ChangePasswordModel
@{
    ViewBag.Title = "Change Password";
}
<div class="row">
    <div class="col-lg-12">
        @using (Html.BeginForm("ChangePassword", "Account", FormMethod.Post, new {@class = ""}))
        {
            @Html.ValidationSummary()
            @Html.UserProfileTextBoxFor(m => m.CurrentPassword, true, true)
            @Html.UserProfileTextBoxFor(m => m.NewPassword, true, true)
            @Html.UserProfileTextBoxFor(m => m.NewPasswordConfirmed, true, true)
            <div class="form-group pull-right">
                <div class="">
                    <button class="btn btn-default">Cancel</button>
                    <button type="submit" class="btn btn-primary">Save</button>
                </div>
            </div>
        }
    </div>
</div>