@model Blog.Web.Models.Account.EditUserProfileModel
@{
    ViewBag.Title = "My Profile";
}
<div class="row">
    <div class="col-lg-12">
        @using (Html.BeginForm("EditProfile", "Account", FormMethod.Post, new {@class = ""}))
        {
            @Html.CustomValidationSummary()
            @Html.HiddenFor(m => m.Id)
            @Html.HiddenFor(m => m.UserName)
            @Html.TextBoxWithLabelFor(m => m.DisplayName)
            @Html.ActionLink("Change Password", "ChangePassword", "Account", new {area = ""}, new {@class = "form-group"})
            @Html.TextBoxWithLabelFor(m => m.Email)
            @Html.TextBoxWithLabelFor(m => m.Street)
            @Html.TextBoxWithLabelFor(m => m.HouseNumber)
            @Html.TextBoxWithLabelFor(m => m.Town)
            @Html.TextBoxWithLabelFor(m => m.Zip)
            @Html.TextBoxWithLabelFor(m => m.Forename)
            @Html.TextBoxWithLabelFor(m => m.LastName)
            <div class="form-group pull-right">
                <div class="">
                    @Html.ActionLink("Cancel", "Index", "Home", new {area = ""}, new {@class = "btn btn-default"})
                    <button type="submit" class="btn btn-primary">Save</button>
                </div>
            </div>
        }
    </div>
</div>